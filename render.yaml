services:
  - type: web
    name: fpl-mcp
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: CACHE_TTL
        value: "300"

